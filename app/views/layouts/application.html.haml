!!!HTML

%head
  %title Dwarf Fortress World Generation Settings Home
  = stylesheet_link_tag :application, :media => :all
  = javascript_include_tag 'application'
  = csrf_meta_tags

%body
  =render 'layouts/navbar'

  .container-fluid
    .row-fluid
      .span3
        =render 'layouts/sidebar'
      .span9
        -if alert
          %p.alert.alert-info= alert
        -if notice
          %p.notice= notice
        = yield

    %footer
      %h4 DO NOT FALL IN LOVE WITH YOUR DATA. This is an alpha release. Features can be added, removed, and data can be dropped without warning.
      %h4 Feedback is welcome. The app is on #{link_to 'github', 'https://github.com/Trevoke/dfworldgen'} and the developer can be reached at 'trevoke_AT_gmail.com'
